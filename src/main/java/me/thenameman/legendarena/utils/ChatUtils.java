package me.thenameman.legendarena.utils;import me.thenameman.legendarena.core.*;import org.bukkit.*;import org.bukkit.command.*;import org.bukkit.entity.*;import org.bukkit.inventory.meta.*;import java.util.*;/** * @author TheNameMan */public class ChatUtils {    public static String getParsedChatMessage(String msg, Rank r) {        if(r == Rank.Helper || r == Rank.Mod || r == Rank.SrMod || r == Rank.Admin || r == Rank.Dev || r == Rank.Owner) { // you know I could simplify this but... eh screw it            String parsedMsg = ChatColor.translateAlternateColorCodes('&', msg);            parsedMsg = ChatColor.BLUE + parsedMsg;            return parsedMsg;        } else {            return msg; // No changes, just return it as is        }    }    public static String getFormattedChat(String msg, Player p) {        String formattedMsg = getParsedChatMessage(msg, Rank.getRank(p));        String name = Rank.getFormattedName(p);        String prefix = Rank.getRankPrefix(Rank.getRank(p));        return getFormattedName(p) + " " + ChatColor.GRAY + PluginUtils.chars[1] + " " + (Rank.getRank(p, Rank.Helper) ? ChatColor.BLUE : ChatColor.GRAY) + msg;    }    public static String getFormattedName(Player p) {        String name = Rank.getFormattedName(p);        String prefix = Rank.getRankPrefix(Rank.getRank(p));        return ChatColor.DARK_GRAY + "{" + prefix + ChatColor.DARK_GRAY + "} {" + name + ChatColor.DARK_GRAY + "}";    }    public static void clearChat(String clearer, String msg) {        if(clearer == null && msg == null)            throw new NullPointerException();        for(int i = 0; i < 120; i++)            broadcast(" ");        if(msg != null)            broadcast(msg);        else            broadcast(ChatColor.BLUE + "Chat was cleared by staff member " + Rank.getFormattedName(Bukkit.getPlayerExact(clearer)) + ChatColor.GREEN + ".");    }    public static void broadcast(String m, Rank r) {        for(Player p : Bukkit.getOnlinePlayers()) {            if(Rank.getRank(p, r)) {                p.sendMessage(m);            }        }    }    public static String playerJoin(String p) {        String joinMsg;        joinMsg = ChatColor.GRAY + "[" + ChatColor.GREEN + "+" + ChatColor.GRAY + "] " + ChatColor.YELLOW + p;        return joinMsg;    }    public static String playerQuit(String p) {        String joinMsg;        joinMsg = ChatColor.GRAY + "[" + ChatColor.RED + "-" + ChatColor.GRAY + "] " + ChatColor.YELLOW + p;        return joinMsg;    }    public static String formatCast(String[] args) {        String cast = "";        for(String s : args) {            if(s.equals("") && args.length == 1) break;            else if(s.equals("")) continue;            cast = cast + s + " ";        }        return cast;    }    public static void shootFireworks(CommandSender sender) {        Player p = (Player) sender;        Firework fw = p.getWorld().spawn(p.getLocation(), Firework.class);        FireworkMeta fm = fw.getFireworkMeta();        Random r = new Random();        FireworkEffect.Type type = getFireworkType();        int c1i = r.nextInt(17) + 1;        int c2i = r.nextInt(17) + 1;        Color c1 = getColour(c1i);        Color c2 = getColour(c2i);        FireworkEffect effect = FireworkEffect.builder().flicker(r.nextBoolean()).withColor(c1).withFade(c2).with(type).trail(r.nextBoolean()).build();        fm.addEffect(effect);        int power = r.nextInt(3) + 1;        fm.setPower(power);        fw.setFireworkMeta(fm);    }    private static FireworkEffect.Type getFireworkType() {        Random r = new Random();        FireworkEffect.Type type;        int fType = r.nextInt(5) + 1;        switch (fType) {            default:            case 1:                type = FireworkEffect.Type.BALL;                break;            case 2:                type = FireworkEffect.Type.STAR;                break;            case 3:                type = FireworkEffect.Type.BALL_LARGE;                break;            case 4:                type = FireworkEffect.Type.CREEPER;                break;            case 5:                type = FireworkEffect.Type.BURST;        }        return type;    }    private static Color getColour(int c) {        switch (c) {            case 1:                return Color.YELLOW;            case 2:                return Color.WHITE;            case 3:                return Color.TEAL;            case 4:                return Color.SILVER;            case 5:                return Color.RED;            case 6:                return Color.PURPLE;            case 7:                return Color.ORANGE;            case 8:                return Color.OLIVE;            case 9:                return Color.NAVY;            case 10:                return Color.MAROON;            case 11:                return Color.LIME;            case 12:                return Color.GREEN;            case 13:                return Color.GRAY;            case 14:                return Color.FUCHSIA;            case 15:                return Color.BLUE;            case 16:                return Color.BLACK;            case 17:                return Color.AQUA;        }        return null;    }    public static void broadcast(String message) {        if(message == null) throw new NullPointerException();        if(Bukkit.getOnlinePlayers().isEmpty()) return; //Do nothing if there are no players online        for(Player pl : Bukkit.getOnlinePlayers()) {            pl.sendMessage(message);        }    }}