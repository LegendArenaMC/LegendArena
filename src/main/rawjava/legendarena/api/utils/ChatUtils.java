/* * This plugin is copyright Legend Arena, 2014-current. See LICENSE.md for full license file. */package legendarena.api.utils;import legendarena.api.exceptions.AreYouDrunkException;import legendarena.api.fanciful.FancyMessage;import legendarena.api.message.Message;import org.bukkit.ChatColor;/** * Various random chat utilities. * * @author ThePixelDev */public class ChatUtils {    private static final ChatColor[] chatColors = {            ChatColor.GREEN,            ChatColor.AQUA,            ChatColor.RED,            ChatColor.BLUE,            ChatColor.DARK_AQUA,            ChatColor.LIGHT_PURPLE,            ChatColor.DARK_PURPLE,            ChatColor.GOLD    };    public static final char[] specialCharacters = {            '♫',            '»',            '♦',            '•',            '■',            '™',            '↔',            '‼',            '♥',            '↨',            '►',            '→',            '‼'    };    public static String formRainbow(String from) {        StringBuilder builder = new StringBuilder();        char[] charList = from.toCharArray();        int colorCount = 0;        for(char c : charList) {            builder.append(chatColors[colorCount]).append(c);            if(colorCount + 1 >= chatColors.length)                colorCount = 0;            else                colorCount++;        }        return builder.toString();    }    @Deprecated    public static String randomRainbow(String from) {        return formRainbow(from);    }    /**     * Try to use fancyMsg() instead. Only use this if you must.     */    public static String getFormattedMsg(String msg, String desc) {        return fancyMsg(msg, desc).toOldMessageFormat();    }    public static FancyMessage fancyMsg(String msg, String desc) {        return new FancyMessage(msg)                    .color(ChatColor.YELLOW)                .then(" " + specialCharacters[1] + " ")                    .color(ChatColor.DARK_GRAY)                .then(desc)                    .color(ChatColor.GREEN);    }    public static FancyMessage fancyHelpMsg(String msg, String desc, String cmd, boolean show) {        FancyMessage built = new FancyMessage(msg)                    .color(ChatColor.YELLOW)                    .command("/" + cmd);        if(show)            built.tooltip("Click to run " + ChatColor.YELLOW + "/" + cmd);        built.then(" " + specialCharacters[1] + " ")                    .color(ChatColor.DARK_GRAY)                .then(desc)                    .color(ChatColor.GREEN);        return built;    }    public static FancyMessage fancyHelpSuggestMsg(String msg, String desc, String suggest, boolean show) {        FancyMessage built = new FancyMessage(msg)                    .color(ChatColor.YELLOW)                .suggest("/" + suggest);        if(show)            built.tooltip(ChatColor.GREEN + "Click to autofill " + ChatColor.YELLOW + "/" + suggest + ChatColor.GREEN + "");        built.then(" " + specialCharacters[1] + " ")                    .color(ChatColor.DARK_GRAY)                .then(desc)                    .color(ChatColor.GREEN);        return built;    }    public static String getFormattedHeader(String header) {        return ChatColor.BLUE + "=-= " + ChatColor.LIGHT_PURPLE + header + ChatColor.BLUE + " =-=";    }    public static void clearChat(String clearer) {        if(clearer == null || clearer.equals(""))            throw new NullPointerException();        String clearMsg = "";        for(int i = 0; i < 120; i++)            clearMsg += "\n ";        new Message().append(clearMsg).broadcast();        new Message().append(getCustomMsg("Chat Management") + "Chat has been cleared by " + ChatColor.RED + clearer + ChatColor.BLUE + ".").broadcast();    }    /**     * Format a cast normally.     * @param args The arguments String[]     * @return The formatted cast     */    public static String formatCast(String[] args) {        if(args == null || args.length == 0 || (args.length == 1 && args[0].equals("")))            throw new NullPointerException("Arguments list cannot be null, empty, or containing only one empty string.");        StringBuilder builder = new StringBuilder();        int runs = 0;        for(String s : args) {            if(s.equals("")) continue;            builder.append(runs > 0 ? " " : "").append(s);            runs++;        }        return builder.toString();    }    /**     * Format a cast, while ignoring certain args[] IDs.     * @param args The arguments String[]     * @param ignoreList The list of IDs to not care about     * @return The formatted cast, without the ignored IDs     */    public static String formatCast(String[] args, int... ignoreList) {        if(ignoreList.length == args.length)            throw new AreYouDrunkException("You can't ignore everything in a cast!");        for(int i : ignoreList) {            if(i < 0 || i > args.length)                throw new AreYouDrunkException("Removal ID is -1 or lower, or more than args.length");            args[i] = "";        }        return formatCast(args);    }    /**     * Get a custom formatted chat prefix.     * @param prefix The prefix to add     * @return The formatted prefix     */    public static String getCustomMsg(String prefix) {        return ChatColor.YELLOW + prefix + " " + ChatColor.DARK_GRAY + specialCharacters[1] + " " + ChatColor.GREEN;    }    /**     * Get a custom formatted chat prefix.     * @param prefix The prefix to add     * @return The formatted prefix     */    public static String getCustomMsg(String prefix, ChatColor prefixColour) {        return prefixColour + prefix + " " + ChatColor.DARK_GRAY + specialCharacters[1] + " " + ChatColor.GREEN;    }    /**     * Get a custom formatted chat prefix.     * @param prefix The prefix to add     * @return The formatted prefix     */    public static String getCustomMsg(String prefix, ChatColor prefixColour, ChatColor messageColour) {        return prefixColour + prefix + " " + ChatColor.DARK_GRAY + specialCharacters[1] + " " + messageColour;    }}