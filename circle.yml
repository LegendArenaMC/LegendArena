general:
  artifacts:
    - build/libs/

# thanks, Gradle...
dependencies:
  override:
    - . ./dummy.sh

# shh...

#test:
  #post:
    #- ./gradlew javadoc
    #- mkdir ./build/out/ && zip ./build/docs/javadoc && mv ./build/libs/*LegendArena*.jar ./build/out/ && mv ./build/docs/**/*.zip ./build/out/