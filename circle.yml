machine:
  java:
    version: oraclejdk8

general:
  artifacts:
    - "target/*.jar"

test:
  override:
    - mvn package:
      parallel: true
      files:
        - target/**/*.class
        - src/main/java/**/*.java 